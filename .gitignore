logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
.dev.vars
.claude/

# Supabase CLI's local project-link cache (created by `supabase link` /
# `supabase functions deploy`) — machine-local state, not shared config.
supabase/.temp/

# Wrangler's local dev state (created by `wrangler pages dev`, used to
# exercise functions/_middleware.js locally) — machine-local, not shared.
.wrangler/
